import React from 'react'
import ReactDOM from 'react-dom/client'
import PeterPanBarMenu from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PeterPanBarMenu />
  </React.StrictMode>,
)
