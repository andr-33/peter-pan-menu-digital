import React from 'react'
import ReactDOM from 'react-dom/client'
import PeterPanBarMenu from './carta_peter_pan_bar'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PeterPanBarMenu />
  </React.StrictMode>,
)
